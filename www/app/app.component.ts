import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'boss-app',
    templateUrl: './app.component.html'
})

export class AppComponent {
    title = 'Boss';
}
